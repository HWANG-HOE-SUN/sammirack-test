# 재고 관리 시스템 개선 지식동기화 문서 v1.1

**문서 목적**: AI가 재고 관리 시스템 개선 작업 시 매번 참조하고 업데이트하여 작업 맥락 유지  
**최종 수정**: 2025-12-13 08:30 (KST)  
**프로젝트**: sammirack-estimator (삼미앵글랙산업 견적/재고 관리 시스템)  
**작업 목표**: 기본 원자재와 기타 추가 옵션 원자재 재고 통합 관리

---

## 📌 필수 규칙

> **AI는 코드 작업 전/후 반드시 이 문서를 읽고 업데이트해야 함**
> 
> 1. 작업 시작 전: 현재 진행 상황 확인
> 2. 작업 완료 후: 작업 히스토리에 기록 (prepend 방식)
> 3. 구현 완료 판단: 반드시 사용자가 테스트 후 승인해야 함 (자의적 완료 판단 절대 금지)
> 4. 문서 양식 자의적 수정 금지
> 5. 기존 변수명/함수명 절대 변경 금지 (사용자 동의 없이)
> 6. 코드 생략 표현 절대 금지 ("// 생략", "동일", "기존과 같음" 등)
> 7. 전체 파일 완전 제공 (누락/생략/반복 없이)
> 8. GitHub 최신 코드 기준으로 작업 (오래된 버전 참조 금지)

---

## 🎯 개선 목표

### 핵심 문제

**현재 상태**:
- 동일한 제품(예: 스텐랙 75 기둥)이 두 가지 partID로 분리 관리됨
  - 기본 원자재: `스텐랙-기둥-높이75`
  - 기타 추가 옵션: `스텐랙-75기둥-`
- 사용자가 기타 추가 옵션으로 선택해도 별도 재고에서 감소
- 실제로는 동일한 창고 재고인데 시스템상 이중 관리
- 하이랙 병합 옵션(예: "80x108 블루선반+오렌지빔")이 하나의 partID로 관리되어 선반과 빔의 개별 재고 추적 불가

**개선 후 목표**:
- 기타 추가 옵션 선택 시 기본 원자재 재고에서 감소
- 하이랙 병합 옵션 자동 분리하여 각 부품 재고 개별 감소
- 재고 관리 컴포넌트에서 통합된 재고 현황 표시
- 100% 정확한 재고 추적

### 핵심 변경사항

| 항목 | 현재 | 개선 후 | 효과 |
|------|------|---------|------|
| 스텐랙 75 기둥 | 2개의 partID로 분리 관리 | 1개의 partID로 통합 | 재고 정확성 향상 |
| 중량랙 60x125 선반 | 2개의 partID로 분리 관리 | 1개의 partID로 통합 | 재고 정확성 향상 |
| 하이랙 80x108 블루선반+오렌지빔 | 1개의 병합 partID | 2개의 개별 partID (선반, 빔) | 개별 부품 재고 추적 |
| 재고 감소 시점 | 기타 추가 옵션 별도 감소 | 기본 원자재 재고 감소 | 이중 관리 방지 |

---

## 📋 전체 작업 계획 (Phase 1-4)

### 작업 현황 테이블

| Phase | 작업명 | 대상 파일 | 작업 내용 | AI 작업 현황 | 사용자 승인 |
|-------|--------|-----------|-----------|--------------|-------------|
| **1-A** | 매핑 테이블 생성 | `src/utils/unifiedPriceManager.js` | EXTRA_TO_BASE_MAPPING 상수 추가<br>스텐랙, 중량랙, 하이랙 전체 매핑<br>병합 옵션 분리 매핑 | 🟢 완료 | ⬜ 대기 |
| **1-B** | BOM 생성 로직 수정 | `src/contexts/ProductContext.jsx` | makeExtraOptionBOM() 함수 수정<br>매핑 테이블 참조하여 기본 원자재 정보 사용<br>병합 옵션 분리 처리 | 🟢 완료 | ⬜ 대기 |
| **2-A** | 재고 감소 로직 수정 | `src/components/InventoryManager.jsx` | deductInventoryOnPrint() 함수 수정<br>BOM 처리 시 매핑 확인<br>병합 옵션 각각 감소 | 🔴 미작업 | ⬜ 대기 |
| **2-B** | 디버깅 로그 추가 | `src/components/InventoryManager.jsx` | 매핑 전/후 partID 로그<br>재고 감소 상세 로그<br>병합 옵션 분리 로그 | 🔴 미작업 | ⬜ 대기 |
| **3-A** | extra_options.json 수정 | `public/extra_options.json` | 하이랙 추가상품6 BOM 분리<br>선반+빔 각각의 정보 명시 | 🔴 미작업 | ⬜ 대기 |
| **3-B** | 재고 컴포넌트 표시 개선 | `src/components/InventoryManager.jsx` | 통합 여부 표시 추가<br>displayName에 통합 정보 표시<br>필터링 개선 | 🔴 미작업 | ⬜ 대기 |
| **4-A** | 스텐랙 테스트 | 전체 시스템 | 기본 옵션 + 기타 추가 옵션<br>재고 감소 확인<br>로그 확인 | 🔴 미작업 | ⬜ 대기 |
| **4-B** | 중량랙 테스트 | 전체 시스템 | 선반 추가 옵션<br>재고 감소 확인 | 🔴 미작업 | ⬜ 대기 |
| **4-C** | 하이랙 270kg 테스트 | 전체 시스템 | 매트그레이/오렌지 선반<br>블루 기둥<br>재고 감소 확인 | 🔴 미작업 | ⬜ 대기 |
| **4-D** | 하이랙 병합 옵션 테스트 | 전체 시스템 | 80x108 블루선반+오렌지빔<br>선반/빔 각각 감소 확인 | 🔴 미작업 | ⬜ 대기 |
| **4-E** | 재고 컴포넌트 표시 테스트 | `InventoryManager.jsx` | 통합 정보 표시 확인<br>재고 수정 정상 작동 확인 | 🔴 미작업 | ⬜ 대기 |

**작업 상태 범례**:
- 🔴 미작업
- 🟡 진행중
- 🟢 완료 (사용자 승인 대기)
- ✅ 완료 (사용자 승인)

---

## 📝 작업 히스토리 (최신순)

### 2025-12-13 08:30 - Phase 1-A, 1-B 완료
- **작업**: 기타 추가 옵션 → 기본 원자재 매핑 시스템 구축
- **Phase 1-A 내용**:
  - `src/utils/unifiedPriceManager.js`에 EXTRA_TO_BASE_MAPPING 테이블 추가
  - mapExtraToBasePart() 함수 구현
  - 스텐랙 11개, 중량랙 12개, 하이랙 45개 매핑 완료
  - 병합 옵션 3개 분리 매핑 (80x108/150/200 선반+빔)
- **Phase 1-B 내용**:
  - `src/contexts/ProductContext.jsx`의 makeExtraOptionBOM() 함수 수정
  - 매핑 테이블 참조하여 기본 원자재로 변환
  - 병합 옵션 자동 분리 처리
  - 관리자 수정 단가 우선 적용 로직 유지
- **실제 inventory.json 데이터 검증**:
  - Gist 서버의 실제 데이터 확인
  - 중량랙 W×D 형식 확인 (w900xd450 등)
  - 하이랙 색상별 분리 확인
- **출력 파일**:
  - `unifiedPriceManager_Phase1A.js` (완전 파일)
  - `ProductContext_Phase1B_makeExtraOptionBOM.jsx` (함수 수정본)
- **상태**: AI 작업 완료, 사용자 승인 대기

### 2025-12-13 01:00 - 중요 오류 수정: 중량랙 partID 구조 정정
- **작업**: 중량랙 매핑 테이블 완전 재작성
- **오류 내용**: 
  - 잘못된 매핑: `중량랙-45x95선반-` → `중량랙-선반-사이즈45x95`
  - 올바른 매핑: `중량랙-45x95선반-` → `중량랙-선반-w900xd450`
- **수정 이유**: 
  - 실제 Gist 데이터는 W×D 형식 사용 (예: w900xd450)
  - 폭(cm)×깊이(cm)를 D(mm)×W(mm)로 변환
  - 45x95 = 폭45cm×깊이95cm → D450×W900
- **추가 수정**:
  - 매핑 제외 항목 명시 (중량바퀴, 합판, 철판형 세트 등)
  - "동격" 원자재만 매핑하는 원칙 명확화
- **영향 파일**:
  - 재고관리시스템_지식동기화_v1.md (매핑 테이블 전면 수정)
  - 재고관리_개선_계획서.md (매핑 테이블 전면 수정)
- **상태**: 문서 수정 완료, 사용자 검토 대기

### 2025-12-13 00:00 - 지식동기화 문서 생성
- **작업**: 재고 관리 시스템 개선 지식동기화 문서 v1.0 작성
- **내용**: 전체 작업 계획 수립, 파일별 수정 포인트 정리, 테스트 시나리오 작성
- **상태**: 문서 작성 완료, 사용자 검토 대기

---

## 🗂️ 프로젝트 구조

### GitHub 저장소: `https://github.com/HHSsub/sammirack-estimator`
### 배포: Vercel (https://sammirack-estimator.vercel.app)

```
sammirack-estimator/
├── src/
│   ├── components/
│   │   ├── InventoryManager.jsx          # 🔥 Phase 2-A, 2-B, 3-B: 재고 감소 로직 수정
│   │   ├── AdminPriceEditor.jsx
│   │   ├── ShortageInventoryPanel.jsx
│   │   └── ...
│   ├── contexts/
│   │   └── ProductContext.jsx            # ✅ Phase 1-B 완료: BOM 생성 로직 수정
│   ├── utils/
│   │   ├── unifiedPriceManager.js        # ✅ Phase 1-A 완료: 매핑 테이블 생성
│   │   ├── realtimeAdminSync.js
│   │   └── materialSort.js
│   └── App.jsx
├── public/
│   ├── extra_options.json                # 🔥 Phase 3-A: 병합 옵션 분리
│   ├── data.json
│   ├── bom_data.json
│   └── all_materials_list_v2.csv
└── README.md
```

---

## 🔧 파일별 상세 수정 포인트

### ✅ Phase 1-A: src/utils/unifiedPriceManager.js - 매핑 테이블 생성 (완료)

**파일 최상단에 추가된 내용**:

```javascript
// ========================================
// ✅ Phase 1-A: 기타 추가 옵션 → 기본 원자재 매핑 테이블
// ========================================
export const EXTRA_TO_BASE_MAPPING = {
  // ========================================
  // 스텐랙 매핑 (11개)
  // ========================================
  '스텐랙-75기둥-': '스텐랙-기둥-높이75',
  '스텐랙-90기둥-': '스텐랙-기둥-높이90',
  '스텐랙-120기둥-': '스텐랙-기둥-높이120',
  '스텐랙-150기둥-': '스텐랙-기둥-높이150',
  '스텐랙-180기둥-': '스텐랙-기둥-높이180',
  '스텐랙-210기둥-': '스텐랙-기둥-높이210',
  '스텐랙-50x75선반-': '스텐랙-선반-사이즈50x75',
  '스텐랙-50x90선반-': '스텐랙-선반-사이즈50x90',
  '스텐랙-50x120선반-': '스텐랙-선반-사이즈50x120',
  '스텐랙-50x150선반-': '스텐랙-선반-사이즈50x150',
  '스텐랙-50x180선반-': '스텐랙-선반-사이즈50x180',
  
  // ========================================
  // 중량랙 매핑 (12개) - W×D 형식
  // ========================================
  // 중요: 45x95 = 폭45cm×깊이95cm = D450×W900
  '중량랙-45x95선반-': '중량랙-선반-w900xd450',
  '중량랙-45x125선반-': '중량랙-선반-w1200xd450',
  '중량랙-45x155선반-': '중량랙-선반-w1500xd450',
  '중량랙-45x185선반-': '중량랙-선반-w1800xd450',
  '중량랙-60x95선반-': '중량랙-선반-w900xd600',
  '중량랙-60x125선반-': '중량랙-선반-w1200xd600',
  '중량랙-60x155선반-': '중량랙-선반-w1500xd600',
  '중량랙-60x185선반-': '중량랙-선반-w1800xd600',
  '중량랙-90x95선반-': '중량랙-선반-w900xd900',
  '중량랙-90x125선반-': '중량랙-선반-w1200xd900',
  '중량랙-90x155선반-': '중량랙-선반-w1500xd900',
  '중량랙-90x185선반-': '중량랙-선반-w1800xd900',
  
  // ========================================
  // 하이랙 270kg 매트그레이 선반 매핑 (6개)
  // ========================================
  '하이랙-45x108매트그레이선반-': '하이랙-선반메트그레이(볼트식)270kg-사이즈45x108270kg',
  '하이랙-45x150매트그레이선반-': '하이랙-선반메트그레이(볼트식)270kg-사이즈45x150270kg',
  '하이랙-45x200매트그레이선반-': '하이랙-선반메트그레이(볼트식)270kg-사이즈45x200270kg',
  '하이랙-60x108매트그레이선반-': '하이랙-선반메트그레이(볼트식)270kg-사이즈60x108270kg',
  '하이랙-60x150매트그레이선반-': '하이랙-선반메트그레이(볼트식)270kg-사이즈60x150270kg',
  '하이랙-60x200매트그레이선반-': '하이랙-선반메트그레이(볼트식)270kg-사이즈60x200270kg',
  
  // ========================================
  // 하이랙 270kg 오렌지 선반 매핑 (6개)
  // ========================================
  '하이랙-45x108선반-': '하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈45x108270kg',
  '하이랙-45x150선반-': '하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈45x150270kg',
  '하이랙-45x200선반-': '하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈45x200270kg',
  '하이랙-60x108선반-': '하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈60x108270kg',
  '하이랙-60x150선반-': '하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈60x150270kg',
  '하이랙-60x200선반-': '하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈60x200270kg',
  
  // ========================================
  // 하이랙 270kg 블루 기둥 매핑 (4개)
  // ========================================
  '하이랙-45x150기둥-': '하이랙-기둥블루(기둥)+오렌지(가로대)(볼트식)270kg-높이150270kg',
  '하이랙-45x200기둥-': '하이랙-기둥블루(기둥)+오렌지(가로대)(볼트식)270kg-높이200270kg',
  '하이랙-60x150기둥-': '하이랙-기둥블루(기둥)+오렌지(가로대)(볼트식)270kg-높이150270kg',
  '하이랙-60x200기둥-': '하이랙-기둥블루(기둥)+오렌지(가로대)(볼트식)270kg-높이200270kg',
  
  // ========================================
  // 하이랙 450kg 매핑 (6개)
  // ========================================
  '하이랙-60x150기둥450kg-': '하이랙-기둥메트그레이(볼트식)450kg-높이150450kg',
  '하이랙-60x200기둥450kg-': '하이랙-기둥메트그레이(볼트식)450kg-높이200450kg',
  '하이랙-60x250기둥450kg-': '하이랙-기둥메트그레이(볼트식)450kg-높이250450kg',
  '하이랙-60x108선반450kg-': '하이랙-선반메트그레이(볼트식)450kg-사이즈60x108450kg',
  '하이랙-60x150선반450kg-': '하이랙-선반메트그레이(볼트식)450kg-사이즈60x150450kg',
  '하이랙-60x200선반450kg-': '하이랙-선반메트그레이(볼트식)450kg-사이즈60x200450kg',
  
  // ========================================
  // 하이랙 600kg 병합 옵션 - 배열로 분리 매핑 (3개)
  // ========================================
  '하이랙-80x108선반+빔-': [
    '하이랙-80x108블루선반-',
    '하이랙-80x108오렌지빔-'
  ],
  '하이랙-80x150선반+빔-': [
    '하이랙-80x150블루선반-',
    '하이랙-80x150오렌지빔-'
  ],
  '하이랙-80x200선반+빔-': [
    '하이랙-80x200블루선반-',
    '하이랙-80x200오렌지빔-'
  ]
  
  // ⚠️ 주의: 아래 항목들은 별도 부품이므로 매핑하지 않음
  // - 중량랙-중량바퀴- (별도 재고 관리)
  // - 파렛트랙-화이트코팅판1000x2440- (합판)
  // - 파렛트랙-월넛코팅판1000x2440- (합판)
  // - 파렛트랙-돌그레이코팅판1000x2440- (합판)
  // - 파렛트랙-일반합판1000x2440- (합판)
  // - 파렛트랙-타이빔1010- (별도 부품)
  // - 파렛트랙-로드빔1460- (타이빔 포함 세트)
  // - 파렛트랙-로드빔2660- (타이빔 포함 세트)
  // - 파렛트랙-철판형1460/2060/2660- (철판형 세트)
  // - 경량랙-기타자재- (기타 자재)
};

// ========================================
// ✅ Phase 1-A: 기타 추가 옵션 partID를 기본 원자재 partID로 변환
// ========================================
export const mapExtraToBasePart = (extraPartId) => {
  const mapped = EXTRA_TO_BASE_MAPPING[extraPartId];
  
  if (Array.isArray(mapped)) {
    // 병합 옵션 - 배열 반환
    console.log(`🔀 병합 옵션 분리: ${extraPartId} → [${mapped.join(', ')}]`);
    return mapped;
  } else if (mapped) {
    // 단일 매핑 - 문자열 반환
    console.log(`🔗 매핑: ${extraPartId} → ${mapped}`);
    return mapped;
  } else {
    // 매핑 없음 - 원래 값 반환
    return extraPartId;
  }
};
```

**export default에 추가**:
```javascript
export default {
  // ... 기존 exports
  mapExtraToBasePart,  // ✅ Phase 1-A 추가
  EXTRA_TO_BASE_MAPPING,  // ✅ Phase 1-A 추가
};
```

---

### ✅ Phase 1-B: src/contexts/ProductContext.jsx - BOM 생성 로직 수정 (완료)

**import 섹션 수정**:

```javascript
import { 
  loadAdminPrices, 
  getEffectivePrice as utilGetEffectivePrice, 
  generatePartId,
  generateInventoryPartId,
  loadExtraOptionsPrices,
  mapExtraToBasePart,  // ✅ Phase 1-B 추가
  EXTRA_TO_BASE_MAPPING  // ✅ Phase 1-B 추가
} from '../utils/unifiedPriceManager';
```

**makeExtraOptionBOM() 함수 완전 교체**:

```javascript
const makeExtraOptionBOM = () => {
  const extraBOM = [];
  
  if (!extraOptionsSel || extraOptionsSel.length === 0) {
    return extraBOM;
  }

  // ✅ extraProducts는 객체 구조: { 랙타입: { 카테고리: [옵션배열] } }
  (Object.values(extraProducts?.[selectedType] || {})).forEach(arr => {
    if (Array.isArray(arr)) {
      arr.forEach(opt => {
        if (extraOptionsSel.includes(opt.id)) {
          // ✅ 1. 원래 inventoryPartId 생성
          const cleanName = (opt.name || '').replace(/\s*\(.*\)\s*/g, '').trim();
          const originalPartId = generateInventoryPartId({ 
            rackType: selectedType, 
            name: cleanName, 
            specification: opt.specification || '',
            colorWeight: opt.colorWeight || ''
          });
          
          console.log(`\n📌 기타 추가 옵션 BOM 처리: ${opt.name}`);
          console.log(`  🔑 원래 partId: "${originalPartId}"`);
          
          // ✅ 2. 매핑 테이블 확인
          const mappedPartIds = mapExtraToBasePart(originalPartId);
          
          if (Array.isArray(mappedPartIds)) {
            // ✅ 병합 옵션 - 각각 추가
            console.log(`  🔀 병합 옵션 분리: ${mappedPartIds.length}개 부품`);
            
            mappedPartIds.forEach((mappedPartId, index) => {
              // 매핑된 partID에서 name과 specification 추출
              const parts = mappedPartId.split('-');
              const mappedName = parts[1] || opt.name;
              const mappedSpec = parts.slice(2).join('-') || '';
              
              // 관리자 수정 단가 우선 사용
              const adminPrices = loadAdminPrices();
              const adminPriceEntry = adminPrices[mappedPartId];
              const extraOptionsPrices = loadExtraOptionsPrices();
              
              // 가격 계산: 관리자 단가 > 추가옵션 단가 / 부품 수 > 기본 가격 / 부품 수
              const effectivePrice = adminPriceEntry && adminPriceEntry.price > 0 
                ? adminPriceEntry.price 
                : (extraOptionsPrices[opt.id]?.price || Number(opt.price) || 0) / mappedPartIds.length;
              
              extraBOM.push({
                rackType: selectedType,
                size: selectedOptions.size || "",
                name: mappedName,
                partId: mappedPartId,
                specification: mappedSpec,
                note: `${opt.name} 분리 ${index + 1}/${mappedPartIds.length}`,
                quantity: Number(opt.quantity) || 1,
                unitPrice: effectivePrice,
                totalPrice: effectivePrice
              });
              
              console.log(`    ✅ 부품 ${index + 1} 추가: ${mappedPartId} (${effectivePrice}원)`);
            });
          } else if (mappedPartIds !== originalPartId) {
            // ✅ 단일 매핑 - 기본 원자재로 교체
            console.log(`  🔗 매핑됨: "${originalPartId}" → "${mappedPartIds}"`);
            
            // 매핑된 partID에서 name과 specification 추출
            const parts = mappedPartIds.split('-');
            const mappedName = parts[1] || opt.name;
            const mappedSpec = parts.slice(2).join('-') || '';
            
            // 관리자 수정 단가 우선 사용
            const adminPrices = loadAdminPrices();
            const adminPriceEntry = adminPrices[mappedPartIds];
            const extraOptionsPrices = loadExtraOptionsPrices();
            
            const effectivePrice = adminPriceEntry && adminPriceEntry.price > 0 
              ? adminPriceEntry.price 
              : (extraOptionsPrices[opt.id]?.price || Number(opt.price) || 0);
            
            extraBOM.push({
              rackType: selectedType,
              size: selectedOptions.size || "",
              name: mappedName,
              partId: mappedPartIds,
              specification: mappedSpec,
              note: opt.note || "",
              quantity: Number(opt.quantity) || 1,
              unitPrice: effectivePrice,
              totalPrice: effectivePrice
            });
            
            console.log(`    ✅ 기본 원자재로 추가: ${mappedPartIds} (${effectivePrice}원)`);
          } else {
            // ✅ 매핑 없음 - 원래 partId 그대로 사용
            console.log(`  ➡️ 매핑 없음: 원래 partId 사용`);
            
            const extraOptionsPrices = loadExtraOptionsPrices();
            const adminPrices = loadAdminPrices();
            const adminPriceEntry = adminPrices[originalPartId];
            
            const effectivePrice = adminPriceEntry && adminPriceEntry.price > 0 
              ? adminPriceEntry.price 
              : (extraOptionsPrices[opt.id]?.price || Number(opt.price) || 0);
            
            extraBOM.push({
              rackType: selectedType,
              size: selectedOptions.size || "",
              name: opt.name,
              partId: originalPartId,
              specification: opt.specification || "",
              note: opt.note || "",
              quantity: Number(opt.quantity) || 1,
              unitPrice: effectivePrice,
              totalPrice: effectivePrice
            });
            
            console.log(`    ✅ 별도 부품으로 추가: ${originalPartId} (${effectivePrice}원)`);
          }
        }
      });
    }
  });
  
  return extraBOM;
};
```

---

## 🔍 테스트 시나리오

### ✅ 시나리오 1: 스텐랙 75 기둥 (단일 매핑)

**테스트 절차**:
1. 스텐랙 선택
2. 기타 추가 옵션에서 "75 기둥" 선택
3. 청구서 인쇄

**예상 결과**:
```
📌 기타 추가 옵션 BOM 처리: 75 기둥
  🔑 원래 partId: "스텐랙-75기둥-"
  🔗 매핑됨: "스텐랙-75기둥-" → "스텐랙-기둥-높이75"
    ✅ 기본 원자재로 추가: 스텐랙-기둥-높이75

📦 재고 감소:
  스텐랙-기둥-높이75: 250 → 246 (-4개)
```

---

### ✅ 시나리오 2: 중량랙 45x125 선반 (W×D 형식 매핑)

**테스트 절차**:
1. 중량랙 선택
2. 기타 추가 옵션에서 "45x125 선반" 선택
3. 청구서 인쇄

**예상 결과**:
```
📌 기타 추가 옵션 BOM 처리: 45x125 선반
  🔑 원래 partId: "중량랙-45x125선반-"
  🔗 매핑됨: "중량랙-45x125선반-" → "중량랙-선반-w1200xd450"
    ✅ 기본 원자재로 추가: 중량랙-선반-w1200xd450

📦 재고 감소:
  중량랙-선반-w1200xd450: 250 → 249 (-1개)
```

---

### ✅ 시나리오 3: 하이랙 80x108 블루선반+오렌지빔 (병합 옵션 분리)

**테스트 절차**:
1. 하이랙 선택
2. 기타 추가 옵션에서 "80x108 블루선반+오렌지빔" 선택
3. 청구서 인쇄

**예상 결과**:
```
📌 기타 추가 옵션 BOM 처리: 80x108 블루선반+오렌지빔
  🔑 원래 partId: "하이랙-80x108선반+빔-"
  🔀 병합 옵션 분리: 2개 부품
    ✅ 부품 1 추가: 하이랙-80x108블루선반- (가격/2)
    ✅ 부품 2 추가: 하이랙-80x108오렌지빔- (가격/2)

📦 재고 감소:
  하이랙-80x108블루선반-: 250 → 249 (-1개)
  하이랙-80x108오렌지빔-: 250 → 249 (-1개)
```

---

## ⚠️ 예상 리스크

### 1. extra_options.json 구조 이슈
- **위험**: extra_options.json의 구조가 예상과 다를 수 있음
- **대응**: Phase 1-B에서 방어 코드 추가 완료 (Array.isArray 체크)

### 2. partID 생성 불일치
- **위험**: generateInventoryPartId()와 실제 inventory.json의 키 불일치
- **대응**: 실제 inventory.json 데이터로 검증 완료
- **검증 결과**: 
  - 중량랙 W×D 형식 확인 ✅
  - 하이랙 색상별 분리 확인 ✅
  - 스텐랙 높이 형식 확인 ✅

### 3. 병합 옵션 가격 분배
- **위험**: 병합 옵션의 가격을 어떻게 나눌지 기준 불명확
- **대응**: Phase 1-B에서 "전체 가격 / 부품 개수" 로직 구현 완료

### 4. 관리자 수정 단가 우선순위
- **위험**: 매핑 후 관리자 단가가 적용되지 않을 수 있음
- **대응**: Phase 1-B에서 매핑된 partID로 관리자 단가 조회 로직 구현 완료

### 5. BOM 데이터 누락
- **위험**: 일부 기타 추가 옵션에 BOM 데이터가 없을 수 있음
- **대응**: Phase 1-B에서 레거시 처리 로직 유지 (opt.name, opt.specification 직접 사용)

---

## 🚫 절대 금지 사항

1. **코드 생략 표현 사용 금지**
   - "// 생략", "// 동일", "// 기존과 같음" 등
   - 반드시 전체 파일 완전 제공

2. **변수명/함수명 임의 변경 금지**
   - `extraOptionsSel` → `selectedExtraOptions` (X)
   - `makeExtraOptionBOM` → `generateExtraOptionBOM` (X)
   - 사용자 동의 없이 절대 변경 금지

3. **자의적 완료 판단 금지**
   - AI가 "완료되었습니다" 선언 금지
   - 반드시 사용자 테스트 후 승인 필요

4. **매핑 테이블 자의적 수정 금지**
   - EXTRA_TO_BASE_MAPPING은 실제 데이터 기반으로만 수정
   - 추측이나 추론으로 매핑 추가 금지

5. **기존 로직 임의 제거 금지**
   - 레거시 처리 로직 유지 필수
   - 하위 호환성 유지

6. **GitHub 오래된 코드 참조 금지**
   - 반드시 최신 main 브랜치 코드 사용
   - 캐시된 코드 사용 금지

7. **사용자 업로드 파일 무시 금지**
   - 사용자가 제공한 inventory.json, extra_options.json 등 우선 참조
   - GitHub 코드와 충돌 시 사용자 파일 우선

---

## 📊 매핑 통계

### 전체 매핑 개수

| 랙 타입 | 매핑 개수 | 병합 옵션 | 매핑 제외 |
|---------|-----------|-----------|-----------|
| 스텐랙 | 11개 | 0개 | 0개 |
| 중량랙 | 12개 | 0개 | 1개 (중량바퀴) |
| 하이랙 270kg | 16개 | 0개 | 0개 |
| 하이랙 450kg | 6개 | 0개 | 0개 |
| 하이랙 600kg | 0개 | 3개 | 0개 |
| 파렛트랙 | 0개 | 0개 | 10개 (합판, 세트) |
| 경량랙 | 0개 | 0개 | 1개 (기타자재) |
| **합계** | **45개** | **3개** | **12개** |

---

## 📁 출력 파일

### Phase 1-A 출력
- `unifiedPriceManager_Phase1A.js` - 완전한 파일 (EXTRA_TO_BASE_MAPPING + mapExtraToBasePart 포함)

### Phase 1-B 출력
- `ProductContext_Phase1B_makeExtraOptionBOM.jsx` - makeExtraOptionBOM() 함수 수정본

### 문서 출력
- `재고관리시스템_지식동기화_v1.1.md` - 업데이트된 지식동기화 문서

---

## 🎯 다음 작업: Phase 2-A, 2-B

**사용자 승인 후 진행**:
1. Phase 2-A: InventoryManager.jsx deductInventoryOnPrint() 수정
2. Phase 2-B: 재고 감소 디버깅 로그 추가

**대기 중**: 사용자의 Phase 1-A, 1-B 테스트 및 승인

---

**문서 버전**: v1.1  
**최종 수정**: 2025-12-13 08:30 KST  
**다음 업데이트**: Phase 1 사용자 승인 후
