# 재고 관리 시스템 개선 계획서 v2.0

## 📋 개요

### 목적
기본 원자재와 기타 추가 옵션 원자재의 재고를 통합 관리하여 재고 관리의 정확성과 효율성을 향상시킴

### 배경
- 현재: 동일한 제품에 대해 기본 원자재와 기타 추가 옵션의 partID가 분리되어 있어 재고가 이중으로 관리됨
- 문제: 사용자가 기타 추가 옵션으로 선택한 제품이 실제로는 기본 원자재와 동일함에도 불구하고 별도 재고로 감소되어 재고 추적 오류 발생
- 하이랙 특수 케이스: 병합된 옵션(예: "블루선반+오렌지빔")이 각각의 부품으로 분리되어 재고 관리되어야 함
- **치명적 문제**: 기타 추가 옵션 체크박스를 체크해도 cart에 전혀 담기지 않음
- **치명적 문제**: 하이랙의 재고관리용 id와 가격관리용 id가 별도인데 전혀 고려하지 못함

---

## 🎯 목표

### 목표 1: 기본 원자재와 기타 추가 옵션 원자재 연동
**구체적 내용:**
- 동일한 제품에 대해 기본 원자재의 partID로 통일하여 재고 관리
- 기타 추가 옵션 선택 시에도 기본 원자재 재고에서 감소
- **하이랙 특수 처리**: cart에 담을 때는 단가관리용 partId 사용, 재고 감소할 때는 재고관리용 inventoryPartId 사용

**대상 랙 종류별 매핑:**

#### 1) 스텐랙
**중요**: 실제 Gist의 기본 원자재 partID는 name에서 숫자를 제거한 형태입니다.

| 기타 추가 옵션 partID | 기본 원자재 partID | 설명 |
|---------------------|-------------------|------|
| `스텐랙-75기둥-` | `스텐랙-기둥-높이75` | 75 기둥 |
| `스텐랙-90기둥-` | `스텐랙-기둥-높이90` | 90 기둥 |
| `스텐랙-120기둥-` | `스텐랙-기둥-높이120` | 120 기둥 |
| `스텐랙-150기둥-` | `스텐랙-기둥-높이150` | 150 기둥 |
| `스텐랙-180기둥-` | `스텐랙-기둥-높이180` | 180 기둥 |
| `스텐랙-210기둥-` | `스텐랙-기둥-높이210` | 210 기둥 |
| `스텐랙-50x75선반-` | `스텐랙-선반-사이즈50x75` | 50x75 선반 |
| `스텐랙-50x90선반-` | `스텐랙-선반-사이즈50x90` | 50x90 선반 |
| `스텐랙-50x120선반-` | `스텐랙-선반-사이즈50x120` | 50x120 선반 |
| `스텐랙-50x150선반-` | `스텐랙-선반-사이즈50x150` | 50x150 선반 |
| `스텐랙-50x180선반-` | `스텐랙-선반-사이즈50x180` | 50x180 선반 |

#### 2) 하이랙 (270kg)
**⚠️ 매우 중요**: 하이랙은 단가 관리용 partID(색상 제거)와 재고 관리용 inventoryPartID(색상 포함)가 분리되어 있습니다.

**매핑 규칙:**
- **기타 추가 옵션 → 기본 원자재 매핑**: 기타 추가 옵션의 inventoryPartId를 기본 원자재의 inventoryPartId로 매핑
- **Cart에 담을 때**: 단가관리용 partId 사용 (색상 제거, generatePartId 사용)
- **재고 감소할 때**: 재고관리용 inventoryPartId 사용 (색상 포함, generateInventoryPartId 사용)

**매트그레이 선반 (추가상품1):**
| 기타 추가 옵션 inventoryPartID | 기본 원자재 inventoryPartID | 단가관리용 partID | 설명 |
|------------------------------|---------------------------|-----------------|------|
| `하이랙-45x108매트그레이선반-` | `하이랙-선반메트그레이(볼트식)270kg-사이즈45x108270kg` | `하이랙-선반-사이즈45x108270kg` | 45x108 매트그레이선반 |
| `하이랙-45x150매트그레이선반-` | `하이랙-선반메트그레이(볼트식)270kg-사이즈45x150270kg` | `하이랙-선반-사이즈45x150270kg` | 45x150 매트그레이선반 |
| `하이랙-45x200매트그레이선반-` | `하이랙-선반메트그레이(볼트식)270kg-사이즈45x200270kg` | `하이랙-선반-사이즈45x200270kg` | 45x200 매트그레이선반 |
| `하이랙-60x108매트그레이선반-` | `하이랙-선반메트그레이(볼트식)270kg-사이즈60x108270kg` | `하이랙-선반-사이즈60x108270kg` | 60x108 매트그레이선반 |
| `하이랙-60x150매트그레이선반-` | `하이랙-선반메트그레이(볼트식)270kg-사이즈60x150270kg` | `하이랙-선반-사이즈60x150270kg` | 60x150 매트그레이선반 |
| `하이랙-60x200매트그레이선반-` | `하이랙-선반메트그레이(볼트식)270kg-사이즈60x200270kg` | `하이랙-선반-사이즈60x200270kg` | 60x200 매트그레이선반 |

**오렌지 선반 (추가상품2):**
| 기타 추가 옵션 inventoryPartID | 기본 원자재 inventoryPartID | 단가관리용 partID | 설명 |
|------------------------------|---------------------------|-----------------|------|
| `하이랙-45x108선반-` | `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈45x108270kg` | `하이랙-선반-사이즈45x108270kg` | 45x108 오렌지선반 |
| `하이랙-45x150선반-` | `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈45x150270kg` | `하이랙-선반-사이즈45x150270kg` | 45x150 오렌지선반 |
| `하이랙-45x200선반-` | `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈45x200270kg` | `하이랙-선반-사이즈45x200270kg` | 45x200 오렌지선반 |
| `하이랙-60x108선반-` | `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈60x108270kg` | `하이랙-선반-사이즈60x108270kg` | 60x108 오렌지선반 |
| `하이랙-60x150선반-` | `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈60x150270kg` | `하이랙-선반-사이즈60x150270kg` | 60x150 오렌지선반 |
| `하이랙-60x200선반-` | `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈60x200270kg` | `하이랙-선반-사이즈60x200270kg` | 60x200 오렌지선반 |

**블루 기둥 (추가상품3):**
| 기타 추가 옵션 inventoryPartID | 기본 원자재 inventoryPartID | 단가관리용 partID | 설명 |
|------------------------------|---------------------------|-----------------|------|
| `하이랙-45x150기둥-` | `하이랙-기둥블루(기둥)+오렌지(가로대)(볼트식)270kg-높이150270kg` | `하이랙-기둥-높이150270kg` | 45x150 블루기둥 |
| `하이랙-45x200기둥-` | `하이랙-기둥블루(기둥)+오렌지(가로대)(볼트식)270kg-높이200270kg` | `하이랙-기둥-높이200270kg` | 45x200 블루기둥 |
| `하이랙-60x150기둥-` | `하이랙-기둥블루(기둥)+오렌지(가로대)(볼트식)270kg-높이150270kg` | `하이랙-기둥-높이150270kg` | 60x150 블루기둥 |
| `하이랙-60x200기둥-` | `하이랙-기둥블루(기둥)+오렌지(가로대)(볼트식)270kg-높이200270kg` | `하이랙-기둥-높이200270kg` | 60x200 블루기둥 |

#### 3) 하이랙 (450kg)
**메트그레이 기둥 및 선반 (추가상품4):**
| 기타 추가 옵션 inventoryPartID | 기본 원자재 inventoryPartID | 단가관리용 partID | 설명 |
|------------------------------|---------------------------|-----------------|------|
| `하이랙-60x150기둥450kg-` | `하이랙-기둥메트그레이(볼트식)450kg-높이150450kg` | `하이랙-기둥-높이150450kg` | 60x150 기둥(450kg) |
| `하이랙-60x200기둥450kg-` | `하이랙-기둥메트그레이(볼트식)450kg-높이200450kg` | `하이랙-기둥-높이200450kg` | 60x200 기둥(450kg) |
| `하이랙-60x250기둥450kg-` | `하이랙-기둥메트그레이(볼트식)450kg-높이250450kg` | `하이랙-기둥-높이250450kg` | 60x250 기둥(450kg) |
| `하이랙-60x108선반450kg-` | `하이랙-선반메트그레이(볼트식)450kg-사이즈60x108450kg` | `하이랙-선반-사이즈60x108450kg` | 60x108 선반(450kg) |
| `하이랙-60x150선반450kg-` | `하이랙-선반메트그레이(볼트식)450kg-사이즈60x150450kg` | `하이랙-선반-사이즈60x150450kg` | 60x150 선반(450kg) |
| `하이랙-60x200선반450kg-` | `하이랙-선반메트그레이(볼트식)450kg-사이즈60x200450kg` | `하이랙-선반-사이즈60x200450kg` | 60x200 선반(450kg) |

#### 4) 중량랙
**중요**: 중량랙은 W×D 형식을 사용합니다. 폭(cm)×깊이(cm) → D(mm)×W(mm)

| 기타 추가 옵션 partID | 기본 원자재 partID | 변환 설명 |
|---------------------|-------------------|---------|
| `중량랙-45x95선반-` | `중량랙-선반-w900xd450` | 폭45cm×깊이95cm → W900×D450 |
| `중량랙-45x125선반-` | `중량랙-선반-w1200xd450` | 폭45cm×깊이125cm → W1200×D450 |
| `중량랙-45x155선반-` | `중량랙-선반-w1500xd450` | 폭45cm×깊이155cm → W1500×D450 |
| `중량랙-45x185선반-` | `중량랙-선반-w1800xd450` | 폭45cm×깊이185cm → W1800×D450 |
| `중량랙-60x95선반-` | `중량랙-선반-w900xd600` | 폭60cm×깊이95cm → W900×D600 |
| `중량랙-60x125선반-` | `중량랙-선반-w1200xd600` | 폭60cm×깊이125cm → W1200×D600 |
| `중량랙-60x155선반-` | `중량랙-선반-w1500xd600` | 폭60cm×깊이155cm → W1500×D600 |
| `중량랙-60x185선반-` | `중량랙-선반-w1800xd600` | 폭60cm×깊이185cm → W1800×D600 |
| `중량랙-90x95선반-` | `중량랙-선반-w900xd900` | 폭90cm×깊이95cm → W900×D900 |
| `중량랙-90x125선반-` | `중량랙-선반-w1200xd900` | 폭90cm×깊이125cm → W1200×D900 |
| `중량랙-90x155선반-` | `중량랙-선반-w1500xd900` | 폭90cm×깊이155cm → W1500×D900 |
| `중량랙-90x185선반-` | `중량랙-선반-w1800xd900` | 폭90cm×깊이185cm → W1800×D900 |

**매핑하지 않는 별도 부품**:
- `중량랙-중량바퀴-` → 별도 재고 관리 (기본 원자재와 다른 격)

### 매핑 제외 항목 (별도 재고 관리)

다음 추가옵션들은 기본 원자재와 "동격"이 아니므로 매핑하지 않고 별도로 재고를 관리합니다:

**중량랙**:
- `중량랙-중량바퀴-` (바퀴 부품)

**파렛트랙/파렛트랙 철판형**:
- `파렛트랙-화이트코팅판1000x2440-` (합판)
- `파렛트랙-월넛코팅판1000x2440-` (합판)
- `파렛트랙-돌코팅판1000x2440-` (합판)
- `파렛트랙-일반합판1000x2440-` (합판)
- `파렛트랙-타이빔1010-` (타이빔 부품)
- `파렛트랙-로드빔1460-` (로드빔+타이빔 세트)
- `파렛트랙-로드빔2660-` (로드빔+타이빔 세트)
- `파렛트랙-철판형1460-` (철판형 세트)
- `파렛트랙-철판형2060-` (철판형 세트)
- `파렛트랙-철판형2660-` (철판형 세트)

**경량랙**:
- `경량랙-기타자재-` (기타 자재)

이들은 `mapExtraToBasePart()` 함수에서 매핑 없이 원래 partID를 그대로 사용합니다.

### 목표 2: 하이랙 병합 옵션 분리 (추가상품6)
**현재 문제:**
- `하이랙-80x108선반+빔-` (80x108 블루선반+오렌지빔)
- `하이랙-80x150선반+빔-` (80x150 블루선반+오렌지빔)
- `하이랙-80x200선반+빔-` (80x200 블루선반+오렌지빔)

**해결 방안:**
각 병합 옵션을 선반과 빔으로 분리하여 개별 재고 관리

**분리 매핑:**
| 병합 옵션 | 분리 부품 1 (재고관리용 inventoryPartId) | 분리 부품 2 (재고관리용 inventoryPartId) | 단가관리용 partId (공통) |
|----------|--------------------------------------|--------------------------------------|---------------------|
| 80x108 블루선반+오렌지빔 | `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)600kg-사이즈80x108600kg` | `하이랙-빔오렌지(볼트식)600kg-사이즈80x108600kg` | `하이랙-선반-사이즈80x108600kg`, `하이랙-빔-사이즈80x108600kg` |
| 80x150 블루선반+오렌지빔 | `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)600kg-사이즈80x150600kg` | `하이랙-빔오렌지(볼트식)600kg-사이즈80x150600kg` | `하이랙-선반-사이즈80x150600kg`, `하이랙-빔-사이즈80x150600kg` |
| 80x200 블루선반+오렌지빔 | `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)600kg-사이즈80x200600kg` | `하이랙-빔오렌지(볼트식)600kg-사이즈80x200600kg` | `하이랙-선반-사이즈80x200600kg`, `하이랙-빔-사이즈80x200600kg` |

**⚠️ 주의**: 실제 inventory.json에는 빔의 재고관리용 ID가 명확하지 않을 수 있으므로, 실제 데이터 확인 필요

---

## 🔧 수정 대상 파일 및 내용

### 1. `src/utils/unifiedPriceManager.js`

#### 수정 위치 1: 매핑 테이블 생성 (파일 최상단)

**수정 이유:**
기타 추가 옵션 partID를 기본 원자재 partID로 매핑하는 테이블 생성

**수정 방법:**
1. `EXTRA_TO_BASE_MAPPING` 테이블 생성 (기타 추가 옵션 inventoryPartId → 기본 원자재 inventoryPartId)
2. `EXTRA_TO_BASE_PARTID_MAPPING` 테이블 생성 (기타 추가 옵션 inventoryPartId → 단가관리용 partId)
3. `mapExtraToBasePart()` 함수 구현 (단일 매핑)
4. `mapExtraToBaseInventoryPart()` 함수 구현 (재고관리용 매핑)
5. `mapExtraToBasePartId()` 함수 구현 (단가관리용 매핑)

**구체적 수정 내용:**
```javascript
// ========================================
// ✅ 기타 추가 옵션 → 기본 원자재 매핑 테이블 (재고관리용)
// ========================================
export const EXTRA_TO_BASE_INVENTORY_MAPPING = {
  // 스텐랙
  '스텐랙-75기둥-': '스텐랙-기둥-높이75',
  '스텐랙-90기둥-': '스텐랙-기둥-높이90',
  // ... (전체 매핑)
  
  // 하이랙 270kg 매트그레이 선반
  '하이랙-45x108매트그레이선반-': '하이랙-선반메트그레이(볼트식)270kg-사이즈45x108270kg',
  // ... (전체 매핑)
  
  // 하이랙 병합 옵션 - 배열로 분리
  '하이랙-80x108선반+빔-': [
    '하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)600kg-사이즈80x108600kg',
    '하이랙-빔오렌지(볼트식)600kg-사이즈80x108600kg'
  ],
  // ...
};

// ========================================
// ✅ 기타 추가 옵션 → 단가관리용 partId 매핑 테이블
// ========================================
export const EXTRA_TO_BASE_PARTID_MAPPING = {
  // 하이랙 270kg 매트그레이 선반
  '하이랙-45x108매트그레이선반-': '하이랙-선반-사이즈45x108270kg',
  // ... (전체 매핑)
};
```

#### 수정 위치 2: 매핑 함수 구현

**구체적 수정 내용:**
```javascript
// 재고관리용 매핑
export const mapExtraToBaseInventoryPart = (extraInventoryPartId) => {
  const mapped = EXTRA_TO_BASE_INVENTORY_MAPPING[extraInventoryPartId];
  if (Array.isArray(mapped)) {
    return mapped; // 병합 옵션 - 배열 반환
  } else if (mapped) {
    return mapped; // 단일 매핑
  } else {
    return extraInventoryPartId; // 매핑 없음
  }
};

// 단가관리용 매핑
export const mapExtraToBasePartId = (extraInventoryPartId) => {
  const mapped = EXTRA_TO_BASE_PARTID_MAPPING[extraInventoryPartId];
  if (mapped) {
    return mapped;
  } else {
    // 매핑 없으면 generatePartId로 생성
    // (하이랙이 아닌 경우)
    return null; // 호출부에서 처리
  }
};
```

### 2. `src/contexts/ProductContext.jsx`

#### 수정 위치: `makeExtraOptionBOM()` 함수 (약 744-787번째 줄)

**수정 이유:**
1. 기타 추가 옵션 선택 시 cart에 제대로 담기도록 수정
2. 하이랙의 경우 단가관리용 partId와 재고관리용 inventoryPartId를 모두 저장
3. 매핑 테이블을 참조하여 기본 원자재 정보로 BOM 생성
4. 병합 옵션 자동 분리 처리

**수정 방법:**
1. 매핑 테이블 import 추가
2. makeExtraOptionBOM() 함수 완전 재작성
3. 하이랙의 경우 partId(단가관리용)와 inventoryPartId(재고관리용) 모두 저장
4. 병합 옵션 분리 처리

**구체적 수정 내용:**
```javascript
import { 
  mapExtraToBaseInventoryPart,
  mapExtraToBasePartId,
  EXTRA_TO_BASE_INVENTORY_MAPPING,
  EXTRA_TO_BASE_PARTID_MAPPING
} from '../utils/unifiedPriceManager';

const makeExtraOptionBOM = () => {
  const extraBOM = [];
  const extraOptionsPrices = loadExtraOptionsPrices();
  const q = Number(quantity) || 1;
  
  if (!extraOptionsSel || extraOptionsSel.length === 0) {
    return extraBOM;
  }
  
  (Object.values(extraProducts?.[selectedType] || {})).forEach(arr => {
    if (Array.isArray(arr)) {
      arr.forEach(opt => {
        if (opt.name && opt.name.includes('기타자재')) {
          return;
        }
        
        if (extraOptionsSel.includes(opt.id)) {
          // 1. 원래 inventoryPartId 생성
          const cleanName = (opt.name || '').replace(/\s*\(.*\)\s*/g, '').trim();
          const originalInventoryPartId = generateInventoryPartId({ 
            rackType: selectedType, 
            name: cleanName, 
            specification: opt.specification || '',
            colorWeight: opt.colorWeight || ''
          });
          
          // 2. 매핑 테이블 확인
          const mappedInventoryPartIds = mapExtraToBaseInventoryPart(originalInventoryPartId);
          
          if (Array.isArray(mappedInventoryPartIds)) {
            // 병합 옵션 - 각각 추가
            mappedInventoryPartIds.forEach((mappedInventoryPartId, index) => {
              // 단가관리용 partId 생성
              const partIdForPrice = mapExtraToBasePartId(mappedInventoryPartId) || 
                generatePartId({ rackType: selectedType, name: cleanName, specification: '' });
              
              // 관리자 수정 단가 우선 사용
              const adminPrices = loadAdminPrices();
              const adminPriceEntry = adminPrices[partIdForPrice];
              
              const effectivePrice = adminPriceEntry && adminPriceEntry.price > 0 
                ? adminPriceEntry.price 
                : (extraOptionsPrices[opt.id]?.price || Number(opt.price) || 0) / mappedInventoryPartIds.length;
              
              extraBOM.push({
                rackType: selectedType,
                size: selectedOptions.size || "",
                name: opt.name,
                partId: partIdForPrice, // 단가관리용
                inventoryPartId: mappedInventoryPartId, // 재고관리용
                specification: opt.specification || "",
                colorWeight: opt.colorWeight || "",
                note: `${opt.name} 분리 ${index + 1}/${mappedInventoryPartIds.length}`,
                quantity: Number(opt.quantity) || 1,
                unitPrice: effectivePrice,
                totalPrice: effectivePrice
              });
            });
          } else if (mappedInventoryPartIds !== originalInventoryPartId) {
            // 단일 매핑 - 기본 원자재로 교체
            const partIdForPrice = mapExtraToBasePartId(mappedInventoryPartIds) || 
              generatePartId({ rackType: selectedType, name: cleanName, specification: '' });
            
            const adminPrices = loadAdminPrices();
            const adminPriceEntry = adminPrices[partIdForPrice];
            
            const effectivePrice = adminPriceEntry && adminPriceEntry.price > 0 
              ? adminPriceEntry.price 
              : (extraOptionsPrices[opt.id]?.price || Number(opt.price) || 0);
            
            extraBOM.push({
              rackType: selectedType,
              size: selectedOptions.size || "",
              name: opt.name,
              partId: partIdForPrice, // 단가관리용
              inventoryPartId: mappedInventoryPartIds, // 재고관리용
              specification: opt.specification || "",
              colorWeight: opt.colorWeight || "",
              note: opt.note || "",
              quantity: Number(opt.quantity) || 1,
              unitPrice: effectivePrice,
              totalPrice: effectivePrice
            });
          } else {
            // 매핑 없음 - 원래 partId 그대로 사용
            const partIdForPrice = generatePartId({ rackType: selectedType, name: cleanName, specification: '' });
            
            const adminPrices = loadAdminPrices();
            const adminPriceEntry = adminPrices[partIdForPrice];
            
            const effectivePrice = adminPriceEntry && adminPriceEntry.price > 0 
              ? adminPriceEntry.price 
              : (extraOptionsPrices[opt.id]?.price || Number(opt.price) || 0);
            
            extraBOM.push({
              rackType: selectedType,
              size: selectedOptions.size || "",
              name: opt.name,
              partId: partIdForPrice,
              inventoryPartId: originalInventoryPartId,
              specification: opt.specification || "",
              colorWeight: opt.colorWeight || "",
              note: opt.note || "",
              quantity: Number(opt.quantity) || 1,
              unitPrice: effectivePrice,
              totalPrice: effectivePrice
            });
          }
        }
      });
    }
  });
  
  return extraBOM;
};
```

### 3. `src/components/InventoryManager.jsx`

#### 수정 위치: `deductInventoryOnPrint` 함수 (약 40-177번째 줄)

**수정 이유:**
재고 감소 시 하이랙의 경우 inventoryPartId를 사용하도록 수정

**수정 방법:**
1. BOM 항목 처리 시 inventoryPartId 우선 사용
2. inventoryPartId가 없으면 기존 로직 사용 (하위 호환성)

**구체적 수정 내용:**
```javascript
item.bom.forEach((bomItem, bomIndex) => {
  // ✅ 하이랙의 경우 inventoryPartId 우선 사용
  let inventoryPartId;
  if (bomItem.inventoryPartId) {
    inventoryPartId = bomItem.inventoryPartId;
  } else {
    // 기존 로직 (하위 호환성)
    inventoryPartId = generateInventoryPartId({
      rackType: bomItem.rackType || '',
      name: bomItem.name || '',
      specification: bomItem.specification || '',
      colorWeight: bomItem.colorWeight || ''
    });
  }
  
  const requiredQty = Number(bomItem.quantity) || 0;
  const currentStock = Number(serverInventory[inventoryPartId]) || 0;
  
  // 재고 감소 로직...
});
```

### 4. `public/extra_options.json`

#### 수정 이유:
하이랙 추가상품6의 BOM 구조를 선반+빔으로 분리

**수정 방법:**
기존:
```json
{
  "id": "h6-80x108-blue-700-shelf",
  "name": "80x108(블루선반+오렌지빔)",
  "price": 81000,
  "bom": [{ "name": "80x108 블루선반+오렌지빔", "qty": 1 }]
}
```

수정 후:
```json
{
  "id": "h6-80x108-blue-700-shelf",
  "name": "80x108(블루선반+오렌지빔)",
  "price": 81000,
  "bom": [
    { "name": "80x108 블루선반", "qty": 1, "rackType": "하이랙", "specification": "사이즈 80x108 600kg", "colorWeight": "블루(볼트식) 600kg" },
    { "name": "80x108 오렌지빔", "qty": 1, "rackType": "하이랙", "specification": "사이즈 80x108 600kg", "colorWeight": "오렌지(볼트식) 600kg" }
  ]
}
```

### 5. `public/all_materials_list_v2.csv`

#### 수정 이유:
하이랙의 기본 원자재 partid가 제대로 들어가 있지 않아 재고관리 컴포넌트에 제대로 표시되지 않음

**수정 방법:**
하이랙의 기본 원자재들을 CSV에 추가:
- `하이랙-선반메트그레이(볼트식)270kg-사이즈45x108270kg`
- `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)270kg-사이즈45x108270kg`
- `하이랙-기둥메트그레이(볼트식)270kg-높이150270kg`
- `하이랙-기둥블루(기둥)+오렌지(가로대)(볼트식)270kg-높이150270kg`
- ... (전체 기본 원자재 추가)

---

## 🧪 테스트 계획

### 테스트 환경 구성
1. 로컬 개발 환경에서 수정 사항 적용
2. 테스트용 재고 데이터 생성 (Gist 백업 후 테스트 데이터 사용)
3. 각 랙 종류별 샘플 견적서 생성

### 테스트 시나리오

#### 시나리오 1: 스텐랙 기본 원자재와 기타 추가 옵션 통합
**테스트 절차:**
1. 스텐랙 견적서 생성 (기본 옵션: 50x120, 높이 90, 3단)
2. 기타 추가 옵션에서 "50x90 선반" 추가
3. Cart에 담기 확인 (가격 상승 확인)
4. 청구서로 전환 후 인쇄 또는 FAX 전송
5. 재고 감소 확인

**검증 항목:**
- [ ] 기타 추가 옵션 선택 시 cart에 제대로 담기는가?
- [ ] 가격이 올바르게 상승하는가?
- [ ] `스텐랙-선반-사이즈50x90` partID의 재고가 감소했는가?
- [ ] `스텐랙-50x90선반-` partID의 재고는 변하지 않았는가?

#### 시나리오 2: 하이랙 270kg 매트그레이 선반 통합
**테스트 절차:**
1. 하이랙 견적서 생성 (기본 옵션: 45x108, 높이 150, 3단, 매트그레이, 270kg)
2. 기타 추가 옵션 "추가상품1"에서 "45x108 매트그레이선반" 추가
3. Cart에 담기 확인 (가격 상승 확인)
4. 청구서로 전환 후 인쇄
5. 재고 감소 확인

**검증 항목:**
- [ ] 기타 추가 옵션 선택 시 cart에 제대로 담기는가?
- [ ] Cart의 BOM에 partId(단가관리용)와 inventoryPartId(재고관리용)가 모두 저장되는가?
- [ ] `하이랙-선반메트그레이(볼트식)270kg-사이즈45x108270kg` inventoryPartID의 재고가 감소했는가?
- [ ] `하이랙-45x108매트그레이선반-` inventoryPartID의 재고는 변하지 않았는가?

#### 시나리오 3: 하이랙 병합 옵션 분리 (추가상품6)
**테스트 절차:**
1. 하이랙 견적서 생성 (기본 옵션)
2. 기타 추가 옵션 "추가상품6"에서 "80x108(블루선반+오렌지빔)" 추가
3. Cart에 담기 확인 (가격 상승 확인)
4. Cart의 BOM 확인 (선반과 빔이 각각 분리되어 있는지)
5. 청구서로 전환 후 인쇄
6. 재고 감소 확인

**검증 항목:**
- [ ] 기타 추가 옵션 선택 시 cart에 제대로 담기는가?
- [ ] Cart의 BOM에 선반과 빔이 각각 분리되어 있는가?
- [ ] `하이랙-선반블루(기둥)+오렌지(가로대)(볼트식)600kg-사이즈80x108600kg` inventoryPartID의 재고가 감소했는가?
- [ ] 빔의 inventoryPartID의 재고가 감소했는가?
- [ ] 두 부품 각각의 재고가 정확히 1개씩 감소했는가?

#### 시나리오 4: 중량랙 선반 추가 옵션 통합
**테스트 절차:**
1. 중량랙 견적서 생성 (기본 옵션)
2. 기타 추가 옵션 "추가상품1"에서 "45x125 선반" 추가
3. Cart에 담기 확인 (가격 상승 확인)
4. 청구서로 전환 후 인쇄
5. 재고 감소 확인

**검증 항목:**
- [ ] 기타 추가 옵션 선택 시 cart에 제대로 담기는가?
- [ ] `중량랙-선반-w1200xd450` partID의 재고가 감소했는가?
- [ ] `중량랙-45x125선반-` partID의 재고는 변하지 않았는가?

#### 시나리오 5: 재고 관리 컴포넌트 표시 확인
**테스트 절차:**
1. 관리자 계정으로 로그인
2. 재고 관리 탭 접속
3. 하이랙 필터 적용
4. 기본 원자재들이 제대로 표시되는지 확인

**검증 항목:**
- [ ] 하이랙 기본 원자재들이 재고 관리 컴포넌트에 표시되는가?
- [ ] 기타 추가 옵션 항목이 기본 원자재와 통합되어 표시되는가?
- [ ] 재고 수량이 정확히 표시되는가?

---

## 📊 구현 우선순위

### Phase 1: 매핑 테이블 및 함수 생성 (우선순위: 매우 높음)
**예상 소요 시간: 3-4시간**
1. `EXTRA_TO_BASE_INVENTORY_MAPPING` 테이블 생성
2. `EXTRA_TO_BASE_PARTID_MAPPING` 테이블 생성
3. 매핑 함수 구현
4. 스텐랙과 중량랙에 대한 매핑 테스트

### Phase 2: makeExtraOptionBOM() 함수 수정 (우선순위: 매우 높음)
**예상 소요 시간: 4-5시간**
1. makeExtraOptionBOM() 함수 완전 재작성
2. 하이랙 단가관리용/재고관리용 ID 분리 처리
3. 병합 옵션 자동 분리 처리
4. 테스트: 기타 추가 옵션 선택 시 cart에 담기는지 확인

### Phase 3: 하이랙 통합 및 재고 감소 로직 수정 (우선순위: 높음)
**예상 소요 시간: 2-3시간**
1. 하이랙 270kg, 450kg 매핑 추가
2. deductInventoryOnPrint() 함수 수정 (inventoryPartId 우선 사용)
3. 하이랙 재고 감소 테스트

### Phase 4: 병합 옵션 분리 및 extra_options.json 수정 (우선순위: 중간)
**예상 소요 시간: 2-3시간**
1. `extra_options.json` 수정 (추가상품6)
2. 병합 옵션 BOM 분리 로직 구현
3. 분리된 부품 각각의 재고 감소 테스트

### Phase 5: all_materials_list_v2.csv 수정 (우선순위: 중간)
**예상 소요 시간: 2-3시간**
1. 하이랙 기본 원자재 CSV에 추가
2. 재고 관리 컴포넌트 표시 확인
3. 전체 시나리오 통합 테스트

### Phase 6: UI 개선 및 최종 테스트 (우선순위: 낮음)
**예상 소요 시간: 2-3시간**
1. 재고 관리 컴포넌트 표시 개선
2. 전체 시나리오 통합 테스트
3. 프로덕션 배포

---

## ⚠️ 주의사항

### 기존 기능 보호
1. **변수/함수명 절대 변경 금지**
   - `generatePartId` → 유지
   - `generateInventoryPartId` → 유지
   - `deductInventoryOnPrint` → 유지
   - `makeExtraOptionBOM` → 유지
   - 모든 기존 함수 시그니처 유지

2. **Gist 데이터 구조 유지**
   - 기존 partID 키 구조 변경 금지
   - 새로운 매핑은 코드 레벨에서만 처리
   - 서버 데이터는 기존 형식 그대로 유지

3. **하위 호환성 보장**
   - 기존 견적서/청구서 데이터 처리 유지
   - 이전에 생성된 문서도 정상 작동해야 함
   - inventoryPartId가 없는 기존 BOM도 처리 가능해야 함

### 테스트 필수 사항
1. 각 수정 후 즉시 테스트 실행
2. 다른 랙 종류에 영향이 없는지 확인
3. 재고 감소 전후 로그 확인 필수
4. **기타 추가 옵션 선택 시 cart에 담기는지 반드시 확인**

### 롤백 계획
1. 각 Phase별로 Git 커밋 분리
2. Gist 데이터 백업 (수정 전)
3. 문제 발생 시 즉시 이전 커밋으로 복원

---

## 📝 추가 확인 필요 사항

### 질문 1: 하이랙 빔의 재고관리용 ID
- 실제 inventory.json에 빔의 재고관리용 ID가 어떻게 되어 있는지 확인 필요
- 예: `하이랙-빔오렌지(볼트식)600kg-사이즈80x108600kg` 형태인지 확인

### 질문 2: all_materials_list_v2.csv 수정 방식
- CSV 파일에 "통합 여부" 또는 "매핑된 partID" 컬럼 추가가 필요한가?
- 아니면 코드 레벨에서만 매핑 처리할 것인가?

### 질문 3: 재고 관리 컴포넌트 표시 방식
- 기타 추가 옵션 항목을 재고 관리 컴포넌트에서 어떻게 표시할 것인가?
  - 옵션 A: 기본 원자재로 통합하여 하나의 행으로만 표시
  - 옵션 B: 기본 원자재와 기타 추가 옵션 모두 표시하되 "통합됨" 표시 추가
  - 옵션 C: 현재 유지 (별도 표시하되 재고는 기본 원자재에서 감소)

---

## 🎉 완료 기준

### 기능 완료 체크리스트
- [ ] 모든 랙 종류의 기타 추가 옵션 → 기본 원자재 매핑 완료
- [ ] 하이랙 병합 옵션 분리 완료
- [ ] **기타 추가 옵션 선택 시 cart에 제대로 담기는지 확인**
- [ ] **하이랙의 단가관리용/재고관리용 ID 분리 처리 완료**
- [ ] 재고 감소 로직 정확성 100% 검증
- [ ] 모든 테스트 시나리오 통과
- [ ] 디버깅 로그 정상 출력
- [ ] Gist 서버 재고 동기화 확인
- [ ] all_materials_list_v2.csv에 하이랙 기본 원자재 추가 완료
- [ ] 프로덕션 배포 완료

### 문서화 완료 체크리스트
- [ ] 코드 주석 추가 (수정 이유, 매핑 규칙 설명)
- [ ] README 업데이트 (새로운 재고 관리 방식 설명)
- [ ] 관리자 매뉴얼 업데이트
- [ ] 테스트 결과 보고서 작성

---

## 📌 다음 단계

1. **사용자 승인 요청**
   - 위 계획서 검토 요청
   - 질문 사항에 대한 답변 요청
   - 우선순위 조정 여부 확인

2. **상세 매핑 테이블 완성**
   - 사용자 확인 후 전체 매핑 테이블 작성
   - 특수 케이스 처리 방안 확정

3. **Phase 1 구현 시작**
   - 승인 후 즉시 매핑 테이블 및 함수 구현
   - 테스트 후 결과 보고

---

**작성일:** 2025년 (현재)  
**작성자:** Claude (AI Assistant)  
**버전:** 2.0
